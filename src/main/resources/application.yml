spring:
  devtools:
    livereload:
      enabled: true
  thymeleaf:
    suffix: .html
    prefix:
      classpath: /templates/
#    아래 적용하면 동작시 화면 수정이 적용된다
  sql:
    init:
      mode: always

#mybatis 파일 경로 설정
mybatis:
  mapper-locations: mapper/**/*.xml

Dspring:
  profiles:
    active: local

project:
  level: IPTABLECOMMANDCODE_LOCAL