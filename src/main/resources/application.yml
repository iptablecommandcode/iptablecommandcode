spring:
  devtools:
    livereload:
      enabled: true
  thymeleaf:
    suffix: .html
    prefix:
      classpath: /templates/
#  mysql 연결
  datasource:
    url: jdbc:mysql://freash97.synology.me:43306/DEVELOPE?useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: developer
    password: deve10perUser!
#    아래 적용하면 동작시 화면 수정이 적용된다
  sql:
    init:
      mode: always
  #spring security 커스텀 보안서비스 제한
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
#mybatis 파일 경로 설정
mybatis:
  mapper-locations: mapper/**/*.xml

Dspring:
  profiles:
    active: local
#
project:
  level: IPTABLECOMMANDCODE_LOCAL
